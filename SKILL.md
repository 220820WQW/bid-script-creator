---
name: bid-script-creator
description: 基于用户提供的目标网址、招投标栏目和采集页数，分析列表页与详情页的真实 HTML 或 JSON 数据来源，在匹配现有真实案例后，生成符合 bbSpider 框架约束的单文件 Python 采集脚本；脚本用于采集同源详情页的 url、title、pubTime、content 及已确认存在的指定类型附件，不负责运行完整采集任务或修改 bbSpider 框架。
---



# SBKJ 招投标脚本生成器



## 核心目标

1. 你是一个资深的python数据采集专家，专注于网页数据采集工作，核心任务是根据用户提供的信息，分析数据，生成Python单文件的数据采集脚本。
2. 脚本抓取的数据主要是以下5种：
   1. url：文章详情页全路径链接（*）。
   2. title：文章标题（*）。
   3. pubTime：文章的发布时间（或创建时间）（*）。【硬性规则】`pubTime` 只提取并保留以下三类仅含年月日的日期格式：`YYYY-M-D`、`YYYY年M月D日`、`YYYY.M.D`，其中月、日均允许一位或两位数字。暂不考虑带时、分、秒的日期时间，也不得提取中文大写日期或其他日期格式。页面日期文本包含前后缀等多余内容时，只允许使用 `extract_and_validate_dates()` 提取上述三类日期。
   4. content：文章正文内容（*）。
   5. 附件：文章附件（没有可忽略），文章正文附件只要`.pdf .doc .docx .xls .xlsx`这5类。
3. 生成脚本前，先阅读本文件中的核心约束；需要确认模板固定结构和常用函数含义时，再阅读 `references/script-contract.md` 辅助说明。
5. 只有发现参数加密、签名、风控、动态 Cookie/Token 等逆向点时，才调用本地 skill 和 MCP 工具进行逆向分析。能还原为无浏览器依赖、可在单文件 Python 脚本中稳定执行的请求链时继续生成；依赖人工验证码、不可移植设备指纹或必须持续运行浏览器时停止生成脚本并说明原因。



## 全局约束

1. 禁止删除或修改 `bbSpider` 框架文件。
3. 禁止修改无关文件；最终交付只允许创建或修改用户指定的采集单文件脚本。逆向分析确有需要时，可以在系统临时目录中生成 JS、响应样本等分析文件，不得将它们作为额外交付物写入采集脚本所在目录。
4. 禁止运行完整采集脚本，避免真实请求目标站点、触发批量采集或影响上传状态。允许使用 `ast.parse` 进行不执行代码、不发起网络请求的本地语法检查；禁止使用会导入或执行脚本的验证方式。
5. 禁止将请求 `URL` 提取为全局变量。
6. 禁止导入不必要的 `typing` 类型注解。
7. 代码实现应尽量简洁，避免过度封装和无意义的通用化。
8. 能固定的参数直接写死在代码里，不要层层传递或抽象成通用参数。



## 网络请求约束

1. 【硬性规则】采集脚本中所有 HTTP/HTTPS 网络发包，无论发生在 `init_func`、`get_list`、`get_content`、全局能力入口还是其内部辅助函数中，都只允许使用 `bbSpider` 提供的 `request` 或固定公用方法 `auto_request`。
2. 业务请求优先使用 `auto_request`。只有 `auto_request` 无法表达必需的框架请求能力时，才允许直接调用 `request.get()` 或 `request.post()`，且仍必须使用从 `bbSpider` 导入的 `request`。
3. 【禁止】不得导入或调用 `requests`、`httpx`、`aiohttp`、`urllib.request`、`urllib3`、`curl_cffi.requests` 等其他 HTTP 客户端发包，不得通过 `subprocess`、`os.system`、`curl`、`wget` 或外部 JS 运行时绕过 `bbSpider.request`。
4. 全局辅助函数可以为完成 Cookie/Token 初始化、风控握手、请求头生成、加密前置数据获取或解密密钥获取而发起必要请求，但请求仍必须遵守本节的发包通道白名单，并属于主流程入口的最小必要调用链。



## 全局作用域约束

1. 全局作用域只允许出现以下内容：
   1. 固定基础导包，以及站点实现确有需要的最少标准库或已安装依赖导包。
   2. 固定公用方法：`auto_request`、`is_same_origin_url`。
   3. 固定全局变量：`HEADERS = {}`、`COOKIES = {}`。
   4. 与 `init_func`、`get_list`、`get_content` 主流程直接相关的必要站点固定常量。
   5. 与 `init_func`、`get_list`、`get_content` 主流程直接相关的站点能力入口函数，以及该入口实现不可缺少的少量内部辅助函数。
2. 【硬性规则】全局辅助代码必须遵循“单一主流程入口 + 最小必要依赖”原则。对同一项站点能力，例如请求参数加密、响应解密或签名，`get_list` 和 `get_content` 原则上只能调用一个全局入口函数。该入口可以再调用完成实现必需的内部辅助函数并使用必需的固定常量。
3. 【硬性规则】只允许保留从主流程入口函数出发能够实际调用到的辅助函数和常量。禁止把逆向过程中完整的 JS 库、通用加密库、调试函数、测试数据、未使用算法分支或与采集主流程无关的工具函数照搬到全局作用域。
4. 【硬性规则】如果某段逻辑只在入口函数内使用一次且可以简洁表达，应直接写在入口函数中，不得拆成新的全局辅助函数。只有算法步骤复杂、需要复用，或拆分后能显著保持加解密实现正确性时，才允许新增入口依赖的内部辅助函数。
5. `HEADERS`、`COOKIES` 默认为空字典。
   1. 如果网站只是简单校验请求头或 Cookie，可以直接写入 `HEADERS`、`COOKIES`。
   2. 如果请求头或 Cookie 中存在动态校验参数，不要硬编码到全局变量。
6. 只有以下情况允许新增全局常量：
   1. RSA、AES 等固定 `KEY`、`IV`、`PUBLIC_KEY`。
   2. MD5 加盐中的固定盐值。
   3. 网站固定不变，且被主流程入口函数或其必需辅助函数实际使用的常量。
7. 允许新增的全局能力入口仅限于主流程必需的固定加密、解密、签名或特殊数据还原入口。加密、解密、签名可以分别作为不同能力各保留一个入口；但同一能力禁止暴露多个平级入口，应由一个语义明确的入口封装完整调用链。
8. 全局常量变量名必须大写；入口函数名必须表达主流程用途，例如 `encrypt_request_data()`、`decrypt_response_data()` 或 `build_request_sign()`，禁止使用 `func1`、`test`、`main_js` 等无业务语义命名。
9. 禁止把栏目 URL、分页 URL、详情 URL、接口 URL 提取成全局变量；这些 URL 必须写在 `payload_list`、`get_list` 或 `get_content` 的实际请求位置。



## HTML静态数据约束

本节只定义 HTML 数据来源的核心原则，具体代码写法、特殊情况必须以 `references/script-get-list.md`、`references/script-get-content.md` 为准。

HTML 数据包括页面直接返回的 HTML，也包括接口返回的 HTML 片段。无论处于目标页面分析、逆向辅助分析、`get_list` 还是 `get_content` 阶段，只要需要解析 HTML 并定位元素，都按本节规则处理。

1. 解析方式

- 必须首先使用 `BeautifulSoup` 和 `html.parser` 解析 HTML 并定位元素。只有 BeautifulSoup 无法解决的内容，例如提取内嵌脚本中非 HTML 结构的字符串数据时，才允许使用 `re` 正则表达式作为补充方案。
- 元素定位优先使用 `select_one` 和 `select`；只有 CSS 选择器无法准确表达时，才允许使用 `find`。
- 元素属性必须使用 `get()` 提取，元素文本必须使用 `get_text(strip=True)` 提取。

2. get_list

- `get_list` 必须从列表项中提取并返回 `url`、`title`、`pubTime` 三个字段，字段名不可更改。
- `url` 必须在 `get_list` 中获取，表示可访问的详情页 URL；如果列表项确实没有详情链接，必须先与用户确认如何构造或替代详情 URL。
- 【硬性规则】每个列表项的链接必须先使用 `urljoin()` 补全为绝对 URL，再使用 `is_same_origin_url()` 与用户给出的目录网站 URL 比较。只有同源链接才允许继续提取 `title`、`pubTime` 并加入 `ret_list`；非同源链接必须直接跳过，不得请求详情页或返回给 `get_content`。
- `title` 和 `pubTime` 可以在 `get_list` 中获取；如果列表页确实缺失，可以临时返回 `None`，并在 `get_content` 中补齐。
- `pubTime` 最终必须包含完整年月日，并严格遵守核心目标中的 `pubTime` 日期格式限制；如果发布时间文本包含多余内容，只允许使用 `extract_and_validate_dates()` 提取规定的三类日期。

3. get_content

- `content` 可以在 `get_content` 中提取，也可以在 `get_list` 中提前返回，必须按页面实际结构决定。
- `content` 必须定位到正文主体区域，禁止直接返回整页 HTML、列表页 HTML 或包含大量导航、页脚、侧栏的外层节点。
- 如果正文中可能包含相对链接，返回前必须使用 `completion_url()` 补全。
- 如果 `get_list` 中的 `title` 或 `pubTime` 为 `None`，必须在 `get_content` 中补齐后再返回。
- 如果正文中存在附件，需要将附件标签也追加到 `content` 中。



## JSON接口数据约束

本节只定义 JSON 接口数据来源的核心原则，具体代码写法、特殊情况必须以 `references/script-get-list.md`、`references/script-get-content.md` 为准。

1. 解析方式

- JSON数据必须先转成Python字典数据。
- 必须根据接口实际响应确认列表字段路径，例如 `data.list`、`data.records`、`rows` 等，脚本中必须按真实路径提取。
- 字段取值必须优先使用字典的 `get()` 方法。

2. get_list

- `get_list` 必须从列表数据中提取并返回 `url`、`title`、`pubTime` 三个字段，字段名不可更改。
- `url` 必须在 `get_list` 中获取，表示可访问的详情页 URL；如果接口没有直接返回详情 URL，可以根据列表中的 ID 或路径字段拼接。
- 【硬性规则】JSON 列表中的详情链接同样必须先补全为绝对 URL，再与用户给出的目录网站 URL 校验同源。只提取同源项，非同源项必须跳过。
- 如果详情接口依赖列表接口中的字段，必须根据真实详情请求所需参数在 `get_list` 中一并返回，供 `get_content` 使用；字段名以实际接口为准，不要固定套用示例字段。
- 如果列表接口已经返回正文 `content`，可以在 `get_list` 中直接返回，后续 `get_content` 按模板规则直接返回。
- `title` 和 `pubTime` 可以在 `get_list` 中获取；如果列表接口确实缺失，可以临时返回 `None`，并在 `get_content` 中补齐。
- `pubTime` 最终必须包含完整年月日，并严格遵守核心目标中的 `pubTime` 日期格式限制；如果接口返回时间戳，必须转成 `YYYY-mm-dd` 格式，不保留时、分、秒。

3. URL 处理

- 最终生成的详情页 `url` 必须是绝对 URL，并且必须与用户给出的目录网站 URL 同源。同源判断的基准是用户给出的目录网站 URL，不是 XHR/Fetch 接口 URL。
- 接口或 HTML 返回绝对 URL 时，必须使用 `is_same_origin_url(详情URL, 用户给出的目录网站URL)` 进行过滤。比较用的目录网站 URL 可以在 `get_list` 中直接写入，不得因此新增全局 URL 常量。
- 接口或 HTML 返回相对 URL 时，必须先使用 `urljoin()` 和用户给出的目录网站 URL 拼接为绝对 URL，再使用 `is_same_origin_url()` 校验同源。

4. get_content

- 正文内容来自 JSON 字段时，可以直接提取字段值作为 `content`。
- 如果正文接口需要列表接口返回的 ID、详情路径或其他参数，必须直接从 `params` 中读取这些字段。
- 如果 JSON 字段中的正文是 HTML 字符串，返回前必须使用 `completion_url()` 补全相对链接。
- 如果 `get_list` 中的 `title` 或 `pubTime` 为 `None`，必须在 `get_content` 中补齐后再返回。
- 如果正文中存在附件，附件信息也存在JSON字段中，则需要给每个附件创建一个 `a` 标签并设置 `href` 属性，然后追加到 `content` 中。如果附件是从其他接口中获取，则需要单独请求获取附件下载链接。



## 工作流程

### step1 确认用户需求

你的目标是收集足够生成 “简单、高相似度招投标采集脚本” 的最小必要信息，不要把需求确认扩展成复杂爬虫分析。

必须从用户输入中识别以下信息：

1. `输出文件`：最终生成或修改的 Python 单文件脚本名称。
2. `目标网址`：列表页入口 URL 或站点首页 URL。
3. `列表页栏目`：需要采集的栏目名称，例如 `通知公告`、`招标公告`、`结果公告`。
4. `页数`：每个栏目需要采集的页数，必须能与栏目一一对应。

如果用户一次提供多个目标网址或多个栏目，必须先整理成明确的采集任务清单，再继续打开网页分析。

整理格式：

```text
输出文件：[文件名]
采集任务：
1. 目标网址：[URL]；栏目：[栏目名]；页数：[N]
2. 目标网址：[URL]；栏目：[栏目名]；页数：[N]
```

缺失信息处理规则：

1. 如果缺少 `输出文件`，必须询问用户补充文件名。
2. 如果缺少 `目标网址`，必须询问用户补充列表页入口或站点首页。
3. 如果缺少 `列表页栏目`，但用户提供的 URL 已经是明确列表页，可以将栏目临时理解为用户描述中的采集对象；无法判断时再询问用户。
4. 如果缺少 `页数`，默认按 `1` 页处理，不必追问。
5. 如果栏目数量和页数数量不一致，必须询问用户确认对应关系。

停止条件：

- 缺少无法从用户描述或页面中稳定推断、且没有明确默认值的必要信息时，必须与用户交互确认。缺少页数时按前述规则默认为 `1` 页，不得因此停止。



### step2 分析目标网页

操作要求：

1. 打开用户提供的 `目标网址`，必要时可使用本地mcp工具打开浏览器进行访问校验测试。
2. 如果用户提供的是站点首页或上级页面，必须在页面中查找并点击对应的 `列表页栏目`，例如 `通知公告`、`招标公告`、`结果公告`。
3. 如果栏目在导航栏、侧边栏、标签页、下拉菜单、更多栏目、分页入口中，必须通过页面实际可见元素进入，不要只凭 URL 猜测。
4. 如果用户提供的 URL 已经是列表页，也要确认当前页面标题、面包屑、栏目高亮或列表内容是否与用户指定栏目一致。
5. 如果页面存在多个同名或近似栏目，必须根据页面位置和列表内容判断最匹配的栏目；无法判断时再询问用户。
6. 【动态栏目 MCP 联调硬性规则】如果同一页面存在多个需要采集的栏目，栏目通过 JavaScript、标签页、按钮等方式动态切换，并且切换栏目后浏览器地址栏 URL 不变，必须使用可用的浏览器 MCP 逐个点击用户指定的栏目并联调。每个栏目都必须同时确认：点击的可见栏目文本与用户给定栏目一致、点击后的栏目高亮或列表内容确实发生对应变化、该次切换触发的 XHR/Fetch 请求或前端数据来源与当前栏目一一对应。禁止只分析默认栏目，禁止仅凭固定页面 URL、接口参数名称或代码猜测栏目对应关系。

列表页分析要求：

1. 观察列表项结构，确认每条数据的 `详情链接`、`标题`、`发布时间` 分别来自哪些 DOM 元素或 JSON 字段。
2. 不得只抽查第一条或某一条列项就判定整个栏目是否可采集。必须检查用户指定页数范围内的列表链接去向；用户未指定页数时，按默认 `1` 页检查。
3. 如果用户要求多页采集，必须尝试点击下一页或观察分页链接，确认页码变化规律。
4. 如果多个栏目都要采集，必须逐个进入栏目页面，并比较列表结构是否一致。
5. 如果多个栏目只是 URL、页码或接口参数不同，但列表项 DOM/JSON 字段一致，后续脚本必须复用同一套解析逻辑。
6. 只有栏目列表结构或接口字段确实不同时，才允许后续使用顶层 `t` 字段做少量分支。
7. 【硬性规则】在用户指定页数范围内，即使大多数列表项跳转到其他域名，只要存在至少一个详情链接与用户给出的目录网站 URL 同源，该栏目就必须继续分析并生成采集逻辑，不得因外链占多数而放弃。
8. 如果用户指定页数范围内所有列表项都与目录网站 URL 非同源，则不应生成对这些外链的采集逻辑，应向用户说明指定范围内没有可采集的同源详情链接。
9. 对动态栏目完成 MCP 联调后，必须分别记录每个栏目的真实接口 URL、请求方式、栏目区分参数、分页参数和响应列表字段路径。如果切换栏目没有产生新请求，必须继续确认数据是否来自首次请求的完整响应、页面内嵌数据或前端缓存，不得直接认定多个栏目使用相同数据。

内容页分析要求：

1. 观察内容正文分别来哪些 DOM 元素或 JSON字段。
2. 必须按用户指定的采集页数抽样同源列表项并打开详情页，检查正文主体之外是否存在 `.pdf`、`.doc`、`.docx`、`.xls`、`.xlsx` 附件，并确认附件的真实下载链接及所在 DOM 或 JSON 字段。只采集 `1` 页时，仅在第一页随机抽样 `3` 条；采集多页时，必须在每个指定页分别随机抽样 `3` 条。任一页不足 `3` 条同源列表项时，必须检查该页全部实际可用条目。
3. 抽样详情页发现额外附件时，必须记录稳定的附件定位和链接提取规则，供 `get_content` 生成附件追加代码。抽样未发现额外附件时，不得为了“可能存在”而生成无依据的通用附件遍历代码。
4. 如果内容页正文本身通过 `iframe`、`object`、`embed` 等标签展示 PDF，该 PDF 属于正文内容，不受“额外附件抽样未发现则不写”规则限制。必须提取真实 PDF 链接，补全为绝对 URL，转换为带有“内容附件”等附件标识文本的 `<a>` 标签，并作为 `content` 的一部分返回。
5. 如果 PDF 真实链接不在 `iframe`、`object`、`embed` 属性中，而是存在于正文内的 JavaScript 调用或脚本字符串中，必须先用 BeautifulSoup 定位正文或相关 `script` 元素，BeautifulSoup 无法直接提取脚本参数时，再使用 `re` 从该局部内容中提取 PDF 链接，并转换为 `<a>` 标签追加到 `content`。
6. 当正文 PDF 与其他独立附件同时存在时，必须同时保留：PDF 转换的 `<a>` 标签作为正文内容，其他附件按实际 DOM 或 JSON 结构追加到 `content`，不得二选一。

数据来源判断要求：

1. 如果列表数据直接存在于页面 HTML 中，按 HTML 静态数据处理。
2. 如果列表数据来自 XHR/Fetch 接口，必须记录真实接口 URL、请求方式、分页参数、请求参数和响应中列表字段路径。
3. 如果页面首屏看不到数据但浏览器渲染后可见，必须判断是否由接口返回后渲染。
4. 如果详情正文直接存在于详情页 HTML 中，按 HTML 静态数据处理。
5. 如果详情正文来自接口，必须记录详情接口 URL、请求参数和正文所在字段。

停止条件：

- 发现逆向点先调用本地 skill 分析。能稳定还原为无浏览器依赖的单文件 Python 请求链时继续生成；依赖人工验证码、不可移植设备指纹、持续浏览器环境，或经分析仍无法稳定还原时，不得强行生成，应说明具体阻断原因。
- 命中“动态栏目 MCP 联调硬性规则”时，如果当前没有可用的浏览器 MCP，或经过逐栏目点击和网络联调后仍无法确认用户给定栏目与真实数据请求的一一对应关系，必须停止生成脚本并说明具体原因，不得猜测栏目参数或套用默认栏目的请求。
- 如果只是普通 HTML 页面、简单 JSON 接口，或多个栏目高度相似，仅需替换 URL、页码、选择器、字段名，则继续生成脚本。



### step3 生成脚本代码

#### 生成前的真实案例校验

完成 step2 的目标网站分析后，必须按以下优先级匹配真实案例，再根据目标站点的真实差异生成代码。不得在尚未确认目标的数据来源、请求方式、分页、列表、详情和附件形态前盲目翻找或套用案例。

1. **优先使用 `references/examples` 中的匹配案例**
   - 【硬性规则】写入任何脚本代码前，必须先阅读与当前目标场景相关的 `references/*.md`，并查看其指向的 `examples/*.py` 真实案例。
   - `examples` 是经过人工筛选的特殊场景案例，优先级高于 `<cwd>/已上传/**`。只要其中存在能够覆盖目标关键实现路径的匹配案例，就必须将该案例作为主参考案例，不得改用 `已上传` 中的普通案例替代。
   - 主参考案例同时用于确认 bbSpider 方法契约、特殊场景实现方式和代码组织风格。生成代码时应保持其整体结构、实现路径及表达方式，再根据目标站点进行最小必要改写。
   - 匹配必须比较 HTML 或 JSON 数据来源、GET/POST 请求方式与参数映射、分页形态、列表与详情解析方式、正文来源，以及目标存在时的附件、内嵌 PDF、加密、签名、风控、动态 Cookie/Token 等特殊能力。不得只因脚本都采集招投标信息就认定匹配。

2. **`references/examples` 无匹配案例时，再检索 `<cwd>/已上传`**
   - 只有 `references/examples` 无法覆盖目标关键实现路径时，才允许在本次运行时当前工作目录的直属子目录 `<cwd>/已上传` 中递归检索真实脚本案例。“当前工作目录”指执行 Skill 时的 `cwd`，不是 Skill 安装目录或输出脚本目录。
   - 应根据 step2 得到的关键特征，先使用文件检索或代码文本检索缩小候选范围，再读取最接近的少量案例；禁止无目的地逐个读取 `<cwd>/已上传/**` 中的全部脚本。
   - 找到匹配案例后，将关键实现路径最接近的一个脚本作为主参考案例，用于确定整体结构和表达风格，包括导包顺序、固定方法、类成员与方法顺序、变量命名习惯、引号、缩进、空行、字典与列表写法，以及请求和解析代码的组织方式。
   - 【边界限制】备选案例只允许来自 `<cwd>/已上传/**`。禁止到当前目录的父目录、兄弟目录、其他工作区、Skill 目录之外的任意路径或网络上寻找案例。

3. **根据目标网站真实结果进行最小必要改写**
   - 最终代码必须以按上述优先级选定的主参考案例为依据，仅按 step2 已确认的目标网站真实情况修改 URL、请求头、Cookie、请求参数、分页参数、选择器、JSON 字段、栏目差异、正文、日期、附件及必要的站点能力逻辑。
   - 除目标站点差异和当前 Skill 硬性规则要求外，不得主动重构主参考案例，不得引入无依据的新封装、设计模式、类型注解或个人偏好的代码组织方式。
   - 主参考案例与当前 Skill 硬性规则冲突时，以当前 Skill 为准；不得复制案例中的失效参数、无用代码、明显错误或违反当前约束的旧写法。
   - 真实案例不得代替 step2 对目标站点真实请求、字段、选择器和附件结构的分析，也不得把案例中的站点参数直接套用到新目标。

4. **生成门槛与停止条件**
   - `references/examples` 无匹配案例，且 `<cwd>/已上传` 目录不存在时，必须立即停止，不得创建或修改目标 Python 脚本，并告知用户因缺少匹配真实案例而未生成脚本。
   - `<cwd>/已上传` 目录存在，但仍未找到能覆盖目标关键实现路径的真实案例时，必须停止，不得凭经验、通用模板或猜测生成，并明确说明缺少哪项实现依据。
   - 只有选定匹配的主参考案例并完成 step2 目标网站真实分析后，才允许生成 `init_func`、`get_list` 和 `get_content`。
   - 本节不可跳过。即使用户要求“直接生成”或“不用查案例”，仍必须完成上述校验。



1. 生成 `init_func` 代码约束:

使用 `payload_list` 描述栏目和分页， `payload_list` 元组必须满足如下规则：

- 元组中每个元素必须是字典类型，每个字典必包含:`url`和`page_number`字段
- 【硬性规则】`payload_list` 和写入 `start_urls` 的任务字典中，所有请求参数只能使用顶层 `data` 字段存储。禁止在其中使用顶层 `params`、`json`、`body`、`form` 或其他字段存储请求参数。
- 【硬性规则】调用 `auto_request` 时，必须按目标站点的真实请求方式将 `params['data']` 映射到对应形参：GET 查询参数使用 `params=params['data']`，POST 表单使用 `data=params['data']`，POST JSON 载荷使用 `json=params['data']`。不得因 `payload_list` 中的字段名为 `data` 就默认发送 POST 表单。
- 【硬性规则】将请求参数从 `payload_list` 写入 `start_urls` 时必须使用 `p['data'].copy()`，避免分页任务共享并修改同一字典。

- 【必写】使用 `#` 号注释，写明该接口所采集的栏目，例如：`通知公告`。
- 【必写】采集接口URL，URL必须是绝对路径，禁止将域名提取到全局变量。
- 【必写】采集页数，必须严格使用 `page_number` 关键字，数据类型必须是 `int` 类型。
- 【可选】栏目类型字段：如果多个栏目结构不一致，需要在 `get_list` 中分支解析，只能在每个字典中使用顶层 `t` 字段区分栏目类型，禁止其他字段名。
- 更细致方法使用案例，必须先查看 `references/script-init-func.md`。



payload_list 定义完成之后，需要往 `start_urls` 列表中写入每个请求：

- 严格遵守使用两个嵌套`for`循环。
- 使用嵌套 `for` 循环的写法，必须遵循模板代码风格。
- 更细致方法使用案例，必须先查看 `references/script-init-func.md`。



2. 生成 `get_list` 代码约束：

- 必须按极简、可阅读、非常容易懂的原则编写。
- 禁止做额外异常捕获，把代码想象成页面和接口可以正常返回预期结构。
- `get_list` 方法的形参名（`params`）禁止修改、增加或删除。
- 返回值必须是`list`类型：
  - 固定使用 `ret_list` 变量名。
  - HTTP 4xx/5xx 时，返回空列表`ret_list`。
- 必须使用 `auto_request` 方法进行发送请求。
- 如果多个栏目列表结构一致，必须使用统一解析逻辑，禁止添加不必要的条件判断。
- 【HTML静态数据】只有页面结构、详情URL拼接方式、标题位置、发布时间位置等确实不同时，才允许按栏目或类型分支。
- 【JSON接口数据】只有接口结构、字段名、详情URL拼接方式等确实不同时，才允许按栏目或类型分支。
- 【JSON接口数据】响应数据是JSON格式时，直接使用 `resp.json()` 转python字典。
- 数据必须严格遵守 HTML 静态数据约束规则 和 JSON接口数据约束规则。
- 更细致方法使用案例，必须先查看 `references/script-get-list.md`。



3. 生成 `get_content` 代码约束：

- 必须按极简、可阅读、非常容易懂的原则编写。
- 禁止做额外异常捕获，把代码想象成页面和接口可以正常返回预期结构。
- `get_content` 方法的形参名（`params`） 禁止修改、增加或删除。
- 返回值必须是`dict`类型或`None`：
  - 常规情况使用 `return` 返回包含 `title`、`pubTime`、`url`、`content` 四个必需字段的字典。
  - 如果 `get_list` 已经获取 `content`，且传入 `get_content` 的 `params` 已包含 `title`、`pubTime`、`url`、`content` 四个必需字段，允许使用 `if params.get('content'):` 后直接 `return params`。也可以显式构造新字典，只要最终结果包含四个必需字段即可。
  - HTTP 4xx/5xx 时，返回 `None`。
- `content` 必须是文章正文内容，有HTML源码则返回源码的字符串格式，无则直接返回文本内容。
- 【条件生成】step2 按指定采集页数执行附件抽样后，只要任一抽样详情页发现额外附件，`get_content` 就必须按已确认的 DOM/JSON 结构提取附件并追加到 `content`。只采集 `1` 页时，以第一页随机抽样的 `3` 个同源详情页为准；采集多页时，以每个指定页分别随机抽样的 `3` 个同源详情页为准；任一页不足 `3` 个时，以该页全部实际可用详情页的检查结果为准。所有抽样均未发现额外附件时，不写额外附件提取代码。
- 【必写例外】正文本身通过 `iframe`、`object`、`embed` 或 JavaScript 展示 PDF 时，无论是否还有额外附件，`get_content` 都必须提取真实 PDF URL，创建带附件标识文本的 `<a>` 标签并加入 `content`。如同时存在其他附件，也必须一并追加。
- `get_content` 需要发起业务请求时，必须使用 `auto_request` 方法。如果 `get_list` 已返回完整的 `title`、`pubTime`、`url`、`content`，则不得为了满足形式而重复请求详情页，可以直接 `return params` 或构造包含四个必需字段的结果返回。
- `title` 或 `pubTime` 如果在 `get_list` 中为 `None`，必须在 `get_content` 中补齐，补齐代码风格必须参考 `references/script-get-content.md`。
- 数据必须严格遵守 HTML 静态数据约束规则 和 JSON接口数据约束规则。
- 更细致方法使用案例，必须先查看 `references/script-get-content.md`。



