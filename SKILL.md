---
name: bid-script-creator
description: 基于用户提供的目标网址、招投标栏目和采集页数，分析列表页与详情页的真实 HTML 或 JSON 数据来源，在参考现有真实案例后，生成符合 bbSpider 框架约束的单文件 Python 采集脚本；脚本用于采集同源详情页的 url、title、pubTime、content 及已确认存在的指定类型附件，不负责运行完整采集任务或修改 bbSpider 框架。
---



# SBKJ 招投标脚本生成器



## 核心目标

你是资深 Python 网页数据采集专家，深耕爬虫开发领域，核心职责：依据用户提供的目标站点信息、采集需求，完成站点数据逻辑分析，输出完整独立、可直接运行的 Python 采集脚本。

任务核心：爬取目标站点指定栏目列表页，对每条列表条目完整采集 5 类数据：title（标题）、url（详情页链接）、pubTime（发布时间）、content（正文内容）、附件。

数据说明：

- title：文章标题。
- url：文章内容页全路径链接。
- pubTime：文章的发布时间或创建时间。
- content：文章正文内容。
- 附件：文章内容页的附件。只要 `.pdf .doc .docx .xls .xlsx` 这5类

生成脚本前，先阅读本文件中的核心约束；需要确认模板固定结构和常用函数含义时，再阅读 `references/script-contract.md` 辅助说明。





## 全局约束

1. 禁止删除或修改 `bbSpider` 框架文件。
3. 禁止修改无关文件；最终交付只允许创建或修改用户指定的采集单文件脚本。逆向分析确有需要时，可以在系统临时目录中生成 JS、响应样本等分析文件，不得将它们作为额外交付物写入采集脚本所在目录。
4. 禁止运行完整采集脚本，避免真实请求目标站点、触发批量采集或影响上传状态。允许使用 `ast.parse` 进行不执行代码、不发起网络请求的本地语法检查；禁止使用会导入或执行脚本的验证方式。
5. 禁止将请求 `URL` 提取为全局变量。
6. 禁止导入不必要的 `typing` 类型注解。
7. 代码实现应尽量简洁，避免过度封装和无意义的通用化。
8. 能固定的参数直接写死在代码里，不要层层传递或抽象成通用参数。



## 网络请求约束

1. 【硬性规则】采集脚本中所有 HTTP/HTTPS 网络发包，无论发生在 `get_list`、`get_content`、全局能力入口还是其内部辅助函数中，都只允许使用固定公用方法 `auto_request` 或 `bbSpider` 提供的 `request` 。
2. 业务请求优先使用 `auto_request`。只有 `auto_request` 无法表达必需的框架请求能力时，才允许直接调用 `request.get()` 或 `request.post()`，且仍必须使用从 `bbSpider` 导入的 `request`。
3. 【禁止】不得导入或调用 `requests`、`httpx`、`aiohttp`、`urllib.request`、`urllib3`、`curl_cffi.requests` 等其他 HTTP 客户端发包，不得通过 `subprocess`、`os.system`、`curl`、`wget` 或外部 JS 运行时绕过 `bbSpider.request`。



## 全局作用域约束

1. 全局作用域除了固定基础导包，运行导入站点实现确有需要的标准库或已安装依赖导包。
2. 【硬性规则】全局辅助代码必须遵循“单一主流程入口 + 最小必要依赖”原则。对同一项站点能力，例如请求参数加密、响应解密或签名，`get_list` 和 `get_content` 原则上只能调用一个全局入口函数。该入口可以再调用完成实现必需的内部辅助函数并使用必需的固定常量。
3. 【硬性规则】只允许保留从主流程入口函数出发能够实际调用到的辅助函数和常量。禁止把逆向过程中完整的 JS 库、通用加密库、调试函数、测试数据、未使用算法分支或与采集主流程无关的工具函数照搬到全局作用域。
4. 【硬性规则】如果某段逻辑只在入口函数内使用一次且可以简洁表达，应直接写在入口函数中，不得拆成新的全局辅助函数。只有算法步骤复杂、需要复用，或拆分后能显著保持加解密实现正确性时，才允许新增入口依赖的内部辅助函数。
5. `HEADERS`、`COOKIES` 默认为空字典。
   - 如果网站只是简单校验请求头或 Cookie，可以直接写入 `HEADERS`、`COOKIES`。
   - 如果请求头或 Cookie 中存在动态校验参数，不要硬编码到全局变量。
6. 只有以下情况允许新增全局常量：
   - RSA、AES 等固定 `KEY`、`IV`、`PUBLIC_KEY`。
   - MD5 加盐中的固定盐值。
   - 网站固定不变，且被主流程入口函数或其必需辅助函数实际使用的常量。
7. 允许新增的全局能力入口仅限于主流程必需的固定加密、解密、签名或特殊数据还原入口。加密、解密、签名可以分别作为不同能力各保留一个入口；但同一能力禁止暴露多个平级入口，应由一个语义明确的入口封装完整调用链。
8. 全局常量变量名必须大写；入口函数名必须表达主流程用途，例如 `encrypt_request_data()`、`decrypt_response_data()` 或 `build_request_sign()`，禁止使用 `func1`、`test`、`main_js` 等无业务语义命名。
9. 禁止把栏目 URL、分页 URL、详情 URL、接口 URL 提取成全局变量；这些 URL 必须写在 `payload_list`、`get_list` 或 `get_content` 的实际请求位置。



## 数据解析方式约束

本节约束目标页面分析、逆向辅助分析、`get_list` 和 `get_content` 中的 HTML 与 JSON 数据解析。必须根据目标站点的真实响应结构选择解析方式，不得混用无依据的选择器、字段路径或正则表达式。

### HTML 数据解析

HTML 数据包括页面直接返回的完整 HTML，以及接口返回的 HTML 片段。

1. 必须优先使用 `BeautifulSoup` 和 `html.parser` 解析 HTML。
2. 禁止使用正则表达式直接解析完整 HTML 结构。
3. 元素定位优先使用 CSS 选择器：
   - 获取单个元素时优先使用 `select_one()`。
   - 获取多个元素时优先使用 `select()`。
   - 只有 CSS 选择器无法准确表达定位条件时，才允许使用 `find()` 或 `find_all()`。
4. 元素属性必须使用 `get()` 提取，元素文本必须使用 `get_text(strip=True)` 提取。
5. 只有需要提取 `script` 标签、JavaScript 调用参数或其他非 HTML 结构的局部字符串，并且 BeautifulSoup 无法直接完成时，才允许对已定位的局部内容使用 `re`。
6. 选择器必须依据目标页面的真实 DOM 结构确定，禁止直接套用案例中的选择器。

### JSON 数据解析

JSON 数据包括接口直接返回的 JSON，以及页面脚本中已确认存在的 JSON 数据。

1. 接口响应为 JSON 时，必须使用 `resp.json()` 转换为 Python 字典或列表；禁止在无必要时先读取 `resp.text` 再使用 `json.loads()`。
2. 必须根据真实响应确认数据类型和字段路径，不得假设 JSON 顶层一定是字典。
3. 必须分别确认列表、详情正文、标题、发布时间和附件所在的真实字段路径，例如 `data.list`、`data.records`、`rows`。
4. 字典字段优先使用 `get()` 读取；列表元素按已确认的索引或遍历关系读取。
5. 多层字段必须按照真实嵌套结构逐层获取，禁止使用未经验证的固定字段路径。
6. 字段缺失时是否返回 `None`、跳过数据或交由 `get_content` 补齐，必须遵守对应的核心方法约束。
7. 禁止直接套用案例中的字段名、字段路径或数据类型。



## CrawlerObject 核心方法约束

本节约束 `CrawlerObject` 中的三个核心方法：`init_func`、`get_list` 和 `get_content`。

### 最终字段约束

本节约束最终返回结果中的字段值。无论字段由 `get_list` 提取、由 `get_content` 补齐，还是在最终结果中重新构造，都必须遵守以下规则。

#### title

1. 最终返回的 `title` 禁止包含 `\r`、`\t`、`\n` 字符；标题中出现这些字符时，优先使用 `handle_str.replace_escape(title)` 处理。
2. 最终返回的 `title` 禁止保留首尾空格，使用字符串的 `strip()` 方法处理即可。
3. 只清理首尾空格，必须保留标题中间原有的空格。
4. 标题同时包含上述字符和首尾空格时，可以使用 `handle_str.replace_escape(title).strip()` 依次处理。

#### pubTime

1. 最终返回的 `pubTime` 必须包含完整年月日。
2. 允许直接返回 `extract_and_validate_dates()` 能提取的三类纯日期格式：`YYYY-M-D`、`YYYY年M月D日`、`YYYY.M.D`，其中月、日允许一位或两位数字。
3. 允许返回不含额外字符的 `YYYY-MM-DD HH:MM` 或 `YYYY-MM-DD HH:MM:SS` 日期时间字符串。
4. 页面或接口已经提供干净的日期时间字符串时，必须原样提取，不必刻意删除或构造时、分、秒。
5. 发布时间文本包含 `发布时间：`、栏目名、来源等额外字符时，使用 `extract_and_validate_dates()` 提取其中的纯日期。
6. JSON 接口返回时间戳时，按 `handle_str.time_stamp()` 的实际结果转换并保留其日期时间，不得强制截断为纯日期。

### init_func

`init_func` 负责使用 `payload_list` 描述栏目、请求 URL、分页数量和请求参数，并将全部列表页任务写入 `start_urls`。具体写法和特殊分页案例必须先查看 `references/script-init-func.md`。

#### payload_list 结构

1. `payload_list` 必须是元组，每个元素必须是字典。
2. 每个任务字典必须包含：
   - `url`：【必写】列表页或列表接口的绝对 URL，禁止只写域名或将域名提取为全局变量。
   - `page_number`：【必写】采集页数，字段名不可更改，值必须是 `int` 类型。
3. 每个任务字典前必须使用 `#` 注释标明所采集的栏目，例如 `# 通知公告`。
4. 只有多个栏目的列表结构或接口字段确实不同时，才允许增加顶层 `t` 字段供 `get_list` 分支；禁止使用其他顶层字段名表示栏目类型。

#### 请求参数

1. 【硬性规则】`payload_list` 和 `start_urls` 任务字典中的所有请求参数只能存放在顶层 `data` 字段中，禁止使用顶层 `params`、`json`、`body`、`form` 或其他字段。
2. 【硬性规则】将请求参数写入 `start_urls` 时，必须使用字典的`copy`方法，例如：`p['data'].copy()`，避免分页任务共享并修改同一字典。
3. 【硬性规则】`get_list` 调用 `auto_request` 时，必须按目标站点的真实请求方式映射 `params['data']`：
   - GET 查询参数：`params=params['data']`。
   - POST 表单：`data=params['data']`。
   - POST JSON 载荷：`json=params['data']`。
4. `data` 只是任务字典中统一的请求参数容器，不代表请求方式；禁止仅根据字段名 `data` 判断应发送 POST 表单。

#### start_urls 生成

1. 必须使用两个嵌套 `for` 循环遍历 `payload_list` 和页码，将全部列表页任务写入 `start_urls`。
2. URL 分页、参数分页以及第一页与后续页 URL 规律不同的情况，都必须在嵌套循环内按真实分页规律处理。
3. 生成代码必须遵循 `references/script-init-func.md` 中的模板风格。



### get_list

`get_list` 负责请求和解析列表数据，并为每条可采集内容返回详情页任务。具体写法和特殊场景必须先查看 `references/script-get-list.md`。

#### 方法接口与返回值

1. 形参名必须固定为 `params`，禁止修改、增加或删除形参。
2. 返回值必须是 `list`，并固定使用 `ret_list` 变量名。
3. HTTP 状态码为 4xx 或 5xx 时，必须返回空列表 `ret_list`。
4. 每个返回字典必须至少包含 `url`、`title`、`pubTime`，字段名不可更改。

#### 请求与解析

1. 业务请求必须使用 `auto_request`，并按 `init_func` 中的请求参数映射规则发送 `params['data']`。
2. JSON 响应必须直接使用 `resp.json()` 转为 Python 字典。
3. HTML 和 JSON 数据的解析必须遵守“数据解析方式约束”。
4. 禁止添加无依据的异常捕获，按已确认的正常响应结构编写简洁、易读的解析代码。

#### url

1. `url` 必须在 `get_list` 中获取，且必须是可访问的详情页绝对 URL。
   - HTML 列表项没有详情链接时，必须先与用户确认如何构造或替代详情 URL。
   - JSON 接口没有直接返回详情 URL 时，可以根据列表中的 ID 或路径字段拼接。
2. 【硬性规则】每个详情链接都必须与用户给出的目录网站 URL 同源：
   - 相对 URL 必须先使用 `urljoin()` 和目录网站 URL 补全，再使用 `is_same_origin_url()` 校验。
   - 绝对 URL 必须直接使用 `is_same_origin_url(详情URL, 目录网站URL)` 校验。
   - 同源判断基准必须是用户给出的目录网站 URL，不得使用 XHR/Fetch 接口 URL。
   - 目录网站 URL 可以直接写在 `get_list` 中，不得因此新增全局 URL 常量。
3. 非同源链接必须立即跳过，不得继续提取字段、加入 `ret_list`、请求详情页或返回给 `get_content`。

#### 字段提取与传递

1. `title` 和 `pubTime` 应优先在 `get_list` 中获取；列表数据确实缺失时，可以返回 `None`，但必须在 `get_content` 中补齐。
2. 列表项中的 `title` 和 `pubTime` 位于同一元素时，必须分别提取，最终 `title` 只能保留纯标题，禁止包含发布时间或其附带文本。
   - 发布时间位于标题元素的独立子元素中，例如 `<a>标题<span>发布时间</span></a>`，必须先从该子元素提取完整的发布时间原文，再从标题中排除该子元素的全部文本；禁止直接对整个 `a` 标签使用 `get_text(strip=True)` 作为 `title`。
   - 标题和发布时间直接混排在同一段文本中，例如 `<a>标题 发布时间</a>`，必须依据目标页面已确认的发布时间原文、稳定格式或固定分隔关系进行拆分，只将发布时间之前或之外的纯标题作为 `title`。
   - 从 `title` 中去除的是页面展示的完整发布时间文本，包括与日期一起出现的 `发布时间：`、栏目名、来源等附带内容；不得只删除纯日期后留下这些无关文字。
   - 禁止仅凭标题中出现数字、短横线或疑似日期片段就盲目删除内容；拆分规则必须来自目标页面的真实稳定结构。
3. `get_list` 提取的 `title` 和 `pubTime` 必须遵守“最终字段约束”。
4. JSON 详情接口依赖列表字段时，必须把真实所需字段一并返回给 `get_content`；字段名按实际接口确定，不得固定套用示例名称。
5. 列表数据已经包含正文时，可以一并返回 `content`；此时返回字典必须包含 `url`、`title`、`pubTime`、`content`。

#### 多栏目分支

1. 多个栏目结构一致时，必须复用同一套解析逻辑，禁止仅因栏目名称不同而增加分支。
2. 只有 HTML 页面结构、详情 URL 拼接方式、标题或日期位置确实不同时，才允许按顶层 `t` 分支。
3. 只有 JSON 响应结构、字段名或详情 URL 拼接方式确实不同时，才允许按顶层 `t` 分支。
4. 每个分支都必须保持简短，并遵守相同的返回字段和同源校验规则。



### get_content

`get_content` 负责获取并整理最终的 `title`、`pubTime`、`url` 和 `content`。具体写法和特殊场景必须先查看 `references/script-get-content.md`。

#### 方法接口与返回值

1. 形参名必须固定为 `params`，禁止修改、增加或删除形参。
2. 正常返回值必须是包含 `title`、`pubTime`、`url`、`content` 四个字段的字典；HTTP 状态码为 4xx 或 5xx 时，必须返回 `None`。
3. `content` 必须是正文内容：存在 HTML 源码时返回 HTML 字符串，否则返回文本内容。
4. 禁止添加无依据的异常捕获，代码必须保持简洁、易读。

#### 请求与正文提取

1. 需要请求详情数据时，必须使用 `auto_request`。
2. `get_list` 已返回完整的 `url`、`title`、`pubTime`、`content` 时，不得重复请求详情页；可以使用 `if params.get('content'):` 后直接 `return params`，也可以显式构造结果字典。
3. HTML 正文必须精确定位正文主体，禁止返回整页 HTML、列表页 HTML 或包含大量导航、页脚、侧栏的外层节点。
4. JSON 正文可以直接从已确认的真实字段中提取。详情接口依赖列表字段时，必须直接从 `params` 读取所需 ID、详情路径或其他参数。
5. 正文解析必须遵守“数据解析方式约束”。

#### 字段补齐与链接处理

1. `get_list` 返回的 `title` 或 `pubTime` 为 `None` 时，必须在 `get_content` 中按 `references/script-get-content.md` 的固定方式补齐。
2. 正文中可能存在相对链接时，返回前必须使用 `completion_url()` 补全；JSON 字段中的正文为 HTML 字符串时同样适用。
3. 最终返回的 `url` 必须保持为文章详情页 URL；如果正文来自独立 JSON 详情接口，不得用接口 URL 替换文章详情页 URL。

#### 附件

1. 【条件生成】只有 step2 的附件抽样发现额外附件时，才生成对应的附件提取代码：
   - 采集 1 页时，检查第一页随机抽取的 3 个同源详情页。
   - 采集多页时，每个指定页分别随机抽取 3 个同源详情页。
   - 某页不足 3 个同源详情页时，检查该页全部实际可用详情页。
   - 任一抽样页发现附件时，必须按已确认的 DOM 或 JSON 结构提取并追加到 `content`；全部抽样均未发现时，禁止生成猜测性的附件遍历代码。
2. JSON 字段包含附件信息时，必须为每个附件创建设置了 `href` 的 `a` 标签，并追加到 `content`。
3. 附件来自独立接口时，必须请求该接口取得真实下载链接，再追加到 `content`。
4. 【必写例外】正文通过 `iframe`、`object`、`embed` 或 JavaScript 展示 PDF 时，无论抽样是否发现额外附件，都必须提取真实 PDF URL，创建带附件标识文本的 `a` 标签并加入 `content`。
5. 正文 PDF 与其他独立附件同时存在时，必须全部保留，不得二选一。





## 工作流程（严格按照顺序执行）

### step1 确认用户需求

你的目标是收集足够生成 “简单、高相似度招投标采集脚本” 的最小必要信息，不要把需求确认扩展成复杂爬虫分析。

必须从用户输入中识别以下信息：

1. `输出文件`：最终生成或修改的 Python 单文件脚本名称。
2. `目标网址`：列表页入口 URL 或站点首页 URL。
3. `列表页栏目`：需要采集的栏目名称，例如 `通知公告`、`招标公告`、`结果公告`。
4. `页数`：每个栏目需要采集的页数，必须能与栏目一一对应。

如果用户一次提供多个目标网址或多个栏目，必须先整理成明确的采集任务清单，再继续打开网页分析。

整理格式：

```text
输出文件：[文件名]
采集任务：
1. 目标网址：[URL]；栏目：[栏目名]；页数：[N]
2. 目标网址：[URL]；栏目：[栏目名]；页数：[N]
```

缺失信息处理规则：

1. 如果缺少 `输出文件`，必须询问用户补充文件名。
2. 如果缺少 `目标网址`，必须询问用户补充列表页入口或站点首页。
3. 如果缺少 `列表页栏目`，但用户提供的 URL 已经是明确列表页，可以将栏目临时理解为用户描述中的采集对象；无法判断时再询问用户。
4. 如果缺少 `页数`，默认按 `1` 页处理，不必追问。
5. 如果栏目数量和页数数量不一致，必须询问用户确认对应关系。

停止条件：

- 缺少无法从用户描述或页面中稳定推断、且没有明确默认值的必要信息时，必须与用户交互确认。缺少页数时按前述规则默认为 `1` 页，不得因此停止。



### step2 分析目标网页

操作要求：

1. 打开用户提供的 `目标网址`，必要时可使用本地mcp工具打开浏览器进行访问校验测试。
2. 如果用户提供的是站点首页或上级页面，必须在页面中查找并点击对应的 `列表页栏目`，例如 `通知公告`、`招标公告`、`结果公告`。
3. 如果栏目在导航栏、侧边栏、标签页、下拉菜单、更多栏目、分页入口中，必须通过页面实际可见元素进入，不要只凭 URL 猜测。
4. 如果用户提供的 URL 已经是列表页，也要确认当前页面标题、面包屑、栏目高亮或列表内容是否与用户指定栏目一致。
5. 如果页面存在多个同名或近似栏目，必须根据页面位置和列表内容判断最匹配的栏目；无法判断时再询问用户。
6. 【动态栏目 MCP 联调硬性规则】如果同一页面存在多个需要采集的栏目，栏目通过 JavaScript、标签页、按钮等方式动态切换，并且切换栏目后浏览器地址栏 URL 不变，必须使用可用的浏览器 MCP 逐个点击用户指定的栏目并联调。每个栏目都必须同时确认：点击的可见栏目文本与用户给定栏目一致、点击后的栏目高亮或列表内容确实发生对应变化、该次切换触发的 XHR/Fetch 请求或前端数据来源与当前栏目一一对应。禁止只分析默认栏目，禁止仅凭固定页面 URL、接口参数名称或代码猜测栏目对应关系。

列表页分析要求：

1. 观察列表项结构，确认每条数据的 `详情链接`、`标题`、`发布时间` 分别来自哪些 DOM 元素或 JSON 字段。
2. 不得只抽查第一条或某一条列项就判定整个栏目是否可采集。必须检查用户指定页数范围内的列表链接去向；用户未指定页数时，按默认 `1` 页检查。
3. 如果用户要求多页采集，必须尝试点击下一页或观察分页链接，确认页码变化规律。
4. 如果多个栏目都要采集，必须逐个进入栏目页面，并比较列表结构是否一致。
5. 如果多个栏目只是 URL、页码或接口参数不同，但列表项 DOM/JSON 字段一致，后续脚本必须复用同一套解析逻辑。
6. 只有栏目列表结构或接口字段确实不同时，才允许后续使用顶层 `t` 字段做少量分支。
7. 【硬性规则】在用户指定页数范围内，即使大多数列表项跳转到其他域名，只要存在至少一个详情链接与用户给出的目录网站 URL 同源，该栏目就必须继续分析并生成采集逻辑，不得因外链占多数而放弃。
8. 如果用户指定页数范围内所有列表项都与目录网站 URL 非同源，则不应生成对这些外链的采集逻辑，应向用户说明指定范围内没有可采集的同源详情链接。
9. 对动态栏目完成 MCP 联调后，必须分别记录每个栏目的真实接口 URL、请求方式、栏目区分参数、分页参数和响应列表字段路径。如果切换栏目没有产生新请求，必须继续确认数据是否来自首次请求的完整响应、页面内嵌数据或前端缓存，不得直接认定多个栏目使用相同数据。

内容页分析要求：

1. 观察内容正文分别来哪些 DOM 元素或 JSON字段。
2. 必须按用户指定的采集页数抽样同源列表项并打开详情页，检查正文主体之外是否存在 `.pdf`、`.doc`、`.docx`、`.xls`、`.xlsx` 附件，并确认附件的真实下载链接及所在 DOM 或 JSON 字段。只采集 `1` 页时，仅在第一页随机抽样 `3` 条；采集多页时，必须在每个指定页分别随机抽样 `3` 条。任一页不足 `3` 条同源列表项时，必须检查该页全部实际可用条目。
3. 抽样详情页发现额外附件时，必须记录稳定的附件定位和链接提取规则，供 `get_content` 生成附件追加代码。抽样未发现额外附件时，不得为了“可能存在”而生成无依据的通用附件遍历代码。
4. 如果内容页正文本身通过 `iframe`、`object`、`embed` 等标签展示 PDF，该 PDF 属于正文内容，不受“额外附件抽样未发现则不写”规则限制。必须提取真实 PDF 链接，补全为绝对 URL，转换为带有“内容附件”等附件标识文本的 `<a>` 标签，并作为 `content` 的一部分返回。
5. 如果 PDF 真实链接不在 `iframe`、`object`、`embed` 属性中，而是存在于正文内的 JavaScript 调用或脚本字符串中，必须先用 BeautifulSoup 定位正文或相关 `script` 元素，BeautifulSoup 无法直接提取脚本参数时，再使用 `re` 从该局部内容中提取 PDF 链接，并转换为 `<a>` 标签追加到 `content`。
6. 当正文 PDF 与其他独立附件同时存在时，必须同时保留：PDF 转换的 `<a>` 标签作为正文内容，其他附件按实际 DOM 或 JSON 结构追加到 `content`，不得二选一。

数据来源判断要求：

1. 如果列表数据直接存在于页面 HTML 中，按 HTML 静态数据处理。
2. 如果列表数据来自 XHR/Fetch 接口，必须记录真实接口 URL、请求方式、分页参数、请求参数和响应中列表字段路径。
3. 如果页面首屏看不到数据但浏览器渲染后可见，必须判断是否由接口返回后渲染。
4. 如果详情正文直接存在于详情页 HTML 中，按 HTML 静态数据处理。
5. 如果详情正文来自接口，必须记录详情接口 URL、请求参数和正文所在字段。

停止条件：

- 【硬性停止】确认目标网站命中以下“瑞树”或“加速乐”特征时，必须立即停止生成脚本，说明命中的防护类型和判断依据；不得继续逆向、不得尝试把验证流程写入采集脚本，也不得因后续请求能够返回 200 而继续生成。
  - **瑞树特征**：首次请求响应状态码为 202 并返回 Cookie；响应页面的 `head` 中存在带 `content` 属性的 `meta` 标签，其值为较长字符串，`meta` 可能有 `id` 属性；页面脚本存在 `$_ts = windows['$_ts'];` 或同类 `$_ts` 代码，并加载内容固定的外链 JavaScript；执行页面 `$_ts` 代码和外链 JavaScript 后会生成新的 Cookie，携带首次响应 Cookie 和新 Cookie 发起第二次请求后响应 200。
  - **加速乐特征**：完成正常访问需要连续三次请求，前两次响应状态码均为 521，第三次响应状态码为 200；第一次响应返回 `__jsluid_s` Cookie，HTML 包含 `document.cookie=('_')+('_')+...` 或同类 JavaScript，执行后生成 `__jsl_clearance_s`；第二次携带这两个 Cookie 请求后仍响应 521，并返回 JavaScript 混淆代码，执行后生成新的 `__jsl_clearance_s`；第三次携带更新后的 Cookie 请求后响应 200。
- 发现逆向点先调用本地 skill 分析。能稳定还原为无浏览器依赖的单文件 Python 请求链时继续生成；依赖人工验证码、不可移植设备指纹、持续浏览器环境，或经分析仍无法稳定还原时，不得强行生成，应说明具体阻断原因。
- 命中“动态栏目 MCP 联调硬性规则”时，如果当前没有可用的浏览器 MCP，或经过逐栏目点击和网络联调后仍无法确认用户给定栏目与真实数据请求的一一对应关系，必须停止生成脚本并说明具体原因，不得猜测栏目参数或套用默认栏目的请求。
- 如果只是普通 HTML 页面、简单 JSON 接口，或多个栏目高度相似，仅需替换 URL、页码、选择器、字段名，则继续生成脚本。



### step3 生成脚本代码

#### 生成前的真实案例参考

完成 step2 的目标网站分析后，应根据目标站点的主要实现特征，从 Skill 的 `examples` 分类目录中查找并阅读最接近的少量真实案例，用于参考 bbSpider 代码结构、方法契约、实现方式和代码风格。真实案例用于辅助生成，不要求与目标站点完全匹配，也不得代替对目标站点真实请求和页面结构的分析。

1. **案例分类与适用场景**
   - `examples/general-cases/`：通用和兜底案例目录。用于存放没有命中独立特殊分类的 HTML、JSON、HTML/JSON 混合采集脚本，也用于存放数量极少、暂不值得建立独立目录的特殊案例。
   - `examples/list-datastore/`：列表数据来自页面 `datastore` 或同类内嵌数据块。
   - `examples/document-write/`：列表项或正文需要解析 `document.write()` 字符串。
   - `examples/list-with-content/`：列表数据已经包含正文，`get_list` 提前返回 `content`。
   - `examples/title-pubtime-mixed/`：标题和发布时间位于同一元素或同一段文本中。
   - `examples/pubtime-composed/`：发布时间分散在多个元素或字段中，需要拼接。
   - `examples/content-fill-pubtime/`：列表数据缺少完整发布时间，需要在 `get_content` 中补齐。
   - `examples/content-fill-title/`：列表数据缺少标题，需要在 `get_content` 中补齐。
   - `examples/multi-layout-branch/`：多个栏目结构不同，需要使用顶层 `t` 分支。
   - `examples/embedded-content-file/`：正文通过 `iframe`、`object`、`embed` 或 JavaScript 展示 PDF 等内容文件。
   - `examples/extra-attachments/`：正文主体之外存在附件区、附件列表或附件接口。
   - `examples/crypto-request-flow/`：存在请求加密、响应解密、签名、动态 Cookie/Token 或其他特殊请求链。

2. **选择参考分类**
   - `examples` 中的脚本按主要实现特征分类。单个脚本可能同时具有多个特征，以所在目录表示的主要特征为准，不要求跨目录重复存放。
   - 目标同时具有多个特征时，优先选择对代码结构影响最大的特征，优先级依次为：加密或特殊请求链、特殊数据来源或还原方式、正文和附件获取方式、多栏目分支或字段补齐、通用案例。
   - 目标站点没有命中任何独立特殊分类时，直接选择 `examples/general-cases/`。
   - 目标命中特殊分类，但对应目录没有接近案例时，继续在 `examples/general-cases/` 中按该特殊特征的关键词检索；仍未找到接近案例时，可以依据已确认的目标网站结果和当前 Skill 约束继续生成。
   - 只有某项特征会明显改变主要代码路径，并且已经存在多个可复用案例或预计会频繁出现时，才建立新的独立分类；否则放入 `examples/general-cases/`。

3. **检索候选案例**
   - 【硬性规则】写入任何脚本代码前，必须先阅读与当前目标场景相关的 `references/*.md`，并查看其指向的分类目录和具体案例。
   - 先根据 step2 确认的主要实现特征选择一个主分类目录，再使用请求方式、分页方式、响应类型、字段路径、正文来源、附件形态和特殊能力等关键词缩小候选范围。
   - 每次只读取最接近的少量案例，禁止无目的地遍历或逐个读取整个 `examples` 目录。
   - `crypto-request-flow` 中的案例差异较大，必须先使用 `encrypt`、`decrypt`、`sign`、`token`、`cookie`、`AES`、`RSA`、`MD5` 等与目标能力相关的关键词检索。
   - 选择参考案例时，应比较数据来源、GET/POST 请求方式与参数映射、分页形态、列表与详情解析方式、正文来源，以及目标存在时的附件、内嵌 PDF、加密、签名、风控、动态 Cookie/Token 等能力。不得只因脚本都采集招投标信息就认定案例具有参考价值。

4. **选择参考案例**
   - 选择一个或少量关键实现路径最接近的脚本作为参考案例，用于确认 bbSpider 方法契约、特殊场景实现方式和整体代码风格，不要求选定唯一主案例。
   - 参考案例可用于借鉴导包顺序、固定方法、类成员与方法顺序、变量命名、引号、缩进、空行、字典与列表写法，以及请求和解析代码的组织方式。
   - references 明确指定的典型案例应优先作为候选，但仍需确认其实现路径与目标站点具有实际参考价值。
   - 未找到高度接近的案例时，不得因此停止生成，也不得为了套用案例而改变已确认的目标站点请求或解析逻辑。

5. **根据目标网站真实结果生成代码**
   - 生成代码的依据优先级依次为：step2 确认的目标网站真实结果、当前 Skill 硬性规则、bbSpider 方法契约、真实案例参考。
   - 必须按照目标网站的真实情况编写 URL、请求头、Cookie、请求参数、分页参数、选择器、JSON 字段、栏目差异、正文、日期、附件及必要的站点能力逻辑。
   - 参考案例与目标网站真实结果、当前 Skill 规则或 bbSpider 方法契约冲突时，不得套用案例；不得复制案例中的失效参数、无用代码、明显错误或违反当前约束的旧写法。
   - 代码应保持与参考案例相近的简洁风格，但不得为了模仿案例而引入无依据的封装、设计模式、类型注解或代码分支。
   - 真实案例不得代替 step2 对目标站点真实请求、字段、选择器和附件结构的分析，也不得把案例中的站点参数直接套用到新目标。

6. **生成门槛**
   - 只有完成 step2 的目标网站真实分析，并确认请求方式、分页规律、列表数据、详情正文及附件来源后，才允许生成 `init_func`、`get_list` 和 `get_content`。
   - 是否存在完全匹配或高度接近的真实案例，不作为生成脚本的必要条件。
   - 未找到高度接近的真实案例时，可以依据已确认的目标网站真实请求、页面结构、bbSpider 方法契约和当前 Skill 约束生成脚本；不得猜测未确认的请求参数、字段路径、选择器或附件结构。
   - 本节的案例参考流程不可完全跳过；即使用户要求“直接生成”或“不用查案例”，仍应在最接近的分类目录中进行必要的快速检索，但不得因缺少匹配案例而停止。



完成真实案例参考后，严格遵守“CrawlerObject 核心方法约束”，依次生成 `init_func`、`get_list` 和 `get_content`。



